{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c '[ \"${HERDR_ENV:-}\" = 1 ] && echo \"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scout, slices to workers; the orchestrator keeps only one-or-two-file changes.\"; true'"
          }
        ]
      }
    ]
  }
}
