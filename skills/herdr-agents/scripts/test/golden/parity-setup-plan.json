{
  "parity: bare plan plans the block and the hooks": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan"
        ],
        "err": "",
        "out": "plan (nothing is written):\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n<ROOT>/repo/.claude/settings.json\n--- a/<ROOT>/repo/.claude/settings.json\n+++ b/<ROOT>/repo/.claude/settings.json\n@@ -1 +1,34 @@\n-{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n+{\n+  \"hooks\": {\n+    \"PreToolUse\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"echo keep-me\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"UserPromptSubmit\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"SessionStart\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ]\n+  }\n+}\n\n",
        "rc": 0
      }
    ]
  },
  "parity: no .gitignore section when the entry is already there": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          ".gitignore",
          "*.log\n.herdr-agents/\n"
        ],
        [
          ".herdr-agents",
          "<dir>"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          ".gitignore",
          "*.log\n.herdr-agents/\n"
        ],
        [
          ".herdr-agents",
          "<dir>"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--session-set",
          "lane.build.kind",
          "pi"
        ],
        "err": "",
        "out": "plan (nothing is written):\n\n<ROOT>/repo/.herdr-agents/ws/session.conf\n  lane.build.kind      (unset) → pi\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n<ROOT>/repo/.claude/settings.json\n--- a/<ROOT>/repo/.claude/settings.json\n+++ b/<ROOT>/repo/.claude/settings.json\n@@ -1 +1,34 @@\n-{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n+{\n+  \"hooks\": {\n+    \"PreToolUse\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"echo keep-me\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"UserPromptSubmit\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"SessionStart\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ]\n+  }\n+}\n\n",
        "rc": 0
      }
    ]
  },
  "parity: plan --no-hooks skips the hooks section": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--no-hooks"
        ],
        "err": "",
        "out": "plan (nothing is written):\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n",
        "rc": 0
      }
    ]
  },
  "parity: plan --panes 4 on a fresh project (nothing written)": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--panes",
          "4"
        ],
        "err": "PROG: warning: lane 'build' kinds differ (implementer=grok designer=agy tasker=grok). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane build=<kind>[:<model>[:<effort>]]'.\nPROG: warning: lane 'review' kinds differ (reviewer=codex security-reviewer=claude ui-reviewer=agy inspector=agy). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane review=<kind>[:<model>[:<effort>]]'.\n",
        "out": "plan (nothing is written):\n\n<ROOT>/repo/.agents/herdr-agents.conf\n  lane.explore.kind    (unset) → grok\n  panes                (unset) → 4\n  lane.build.roles     (unset) → implementer,designer,tasker\n  lane.explore.roles   (unset) → scouter,researcher\n  lane.review.roles    (unset) → reviewer,security-reviewer,ui-reviewer,inspector\n  max_workers          (unset) → 3\n  split_max_panes      (unset) → 4\n  reuse_workers        (unset) → on\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n<ROOT>/repo/.claude/settings.json\n--- a/<ROOT>/repo/.claude/settings.json\n+++ b/<ROOT>/repo/.claude/settings.json\n@@ -1 +1,34 @@\n-{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n+{\n+  \"hooks\": {\n+    \"PreToolUse\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"echo keep-me\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"UserPromptSubmit\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"SessionStart\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ]\n+  }\n+}\n\n",
        "rc": 0
      }
    ]
  },
  "parity: plan --session-set with no session file yet": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--session-set",
          "lane.build.kind",
          "pi"
        ],
        "err": "",
        "out": "plan (nothing is written):\n\n<ROOT>/state/ws/session.conf\n  lane.build.kind      (unset) → pi\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n<ROOT>/repo/.claude/settings.json\n--- a/<ROOT>/repo/.claude/settings.json\n+++ b/<ROOT>/repo/.claude/settings.json\n@@ -1 +1,34 @@\n-{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n+{\n+  \"hooks\": {\n+    \"PreToolUse\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"echo keep-me\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"UserPromptSubmit\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"SessionStart\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ]\n+  }\n+}\n\n",
        "rc": 0
      }
    ]
  },
  "parity: plan --target on a file carrying the block": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# head\n<!-- herdr-agents:start -->\nold block line\n<!-- herdr-agents:end -->\n# tail\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# head\n<!-- herdr-agents:start -->\nold block line\n<!-- herdr-agents:end -->\n# tail\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--target",
          "AGENTS.md"
        ],
        "err": "",
        "out": "plan (nothing is written):\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1,5 +1,42 @@\n # head\n <!-- herdr-agents:start -->\n-old block line\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n <!-- herdr-agents:end -->\n # tail\n\n<ROOT>/repo/.claude/settings.json\n--- a/<ROOT>/repo/.claude/settings.json\n+++ b/<ROOT>/repo/.claude/settings.json\n@@ -1 +1,34 @@\n-{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n+{\n+  \"hooks\": {\n+    \"PreToolUse\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"echo keep-me\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"UserPromptSubmit\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"SessionStart\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ]\n+  }\n+}\n\n",
        "rc": 0
      }
    ]
  },
  "parity: plan --user-set over an existing user value": {
    "after": {
      "conf": [
        [
          "herdr-agents",
          "<dir>"
        ],
        [
          "herdr-agents/config",
          "model.pi.worker=other/model\n"
        ]
      ],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [
        [
          "herdr-agents",
          "<dir>"
        ],
        [
          "herdr-agents/config",
          "model.pi.worker=other/model\n"
        ]
      ],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--user-set",
          "model.pi.worker",
          "new/model"
        ],
        "err": "",
        "out": "plan (nothing is written):\n\n<ROOT>/conf/herdr-agents/config\n  model.pi.worker      other/model → new/model\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n<ROOT>/repo/.claude/settings.json\n--- a/<ROOT>/repo/.claude/settings.json\n+++ b/<ROOT>/repo/.claude/settings.json\n@@ -1 +1,34 @@\n-{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n+{\n+  \"hooks\": {\n+    \"PreToolUse\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"echo keep-me\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"UserPromptSubmit\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"SessionStart\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ]\n+  }\n+}\n\n",
        "rc": 0
      }
    ]
  },
  "parity: plan --user-set with no user file yet": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--user-set",
          "model.pi.worker",
          "my-provider/my-model"
        ],
        "err": "",
        "out": "plan (nothing is written):\n\n<ROOT>/conf/herdr-agents/config\n  model.pi.worker      (unset) → my-provider/my-model\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n<ROOT>/repo/.claude/settings.json\n--- a/<ROOT>/repo/.claude/settings.json\n+++ b/<ROOT>/repo/.claude/settings.json\n@@ -1 +1,34 @@\n-{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n+{\n+  \"hooks\": {\n+    \"PreToolUse\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"echo keep-me\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"UserPromptSubmit\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"SessionStart\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ]\n+  }\n+}\n\n",
        "rc": 0
      }
    ]
  },
  "parity: plan a key the real setup would delete": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".agents",
          "<dir>"
        ],
        [
          ".agents/herdr-agents.conf",
          "role.planner.model=fable\n"
        ],
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".agents",
          "<dir>"
        ],
        [
          ".agents/herdr-agents.conf",
          "role.planner.model=fable\n"
        ],
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--panes",
          "4"
        ],
        "err": "PROG: warning: lane 'build' kinds differ (implementer=grok designer=agy tasker=grok). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane build=<kind>[:<model>[:<effort>]]'.\nPROG: warning: lane 'review' kinds differ (reviewer=codex security-reviewer=claude ui-reviewer=agy inspector=agy). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane review=<kind>[:<model>[:<effort>]]'.\n",
        "out": "plan (nothing is written):\n\n<ROOT>/repo/.agents/herdr-agents.conf\n  role.planner.model   fable → (removed)\n  lane.explore.kind    (unset) → grok\n  panes                (unset) → 4\n  lane.build.roles     (unset) → implementer,designer,tasker\n  lane.explore.roles   (unset) → scouter,researcher\n  lane.review.roles    (unset) → reviewer,security-reviewer,ui-reviewer,inspector\n  max_workers          (unset) → 3\n  split_max_panes      (unset) → 4\n  reuse_workers        (unset) → on\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n<ROOT>/repo/.claude/settings.json\n--- a/<ROOT>/repo/.claude/settings.json\n+++ b/<ROOT>/repo/.claude/settings.json\n@@ -1 +1,34 @@\n-{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n+{\n+  \"hooks\": {\n+    \"PreToolUse\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"echo keep-me\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"UserPromptSubmit\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"SessionStart\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ]\n+  }\n+}\n\n",
        "rc": 0
      }
    ]
  },
  "parity: plan a valid --lane and a --set": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--panes",
          "4",
          "--lane",
          "build=grok:grok-4.7:high",
          "--set",
          "max_workers",
          "5"
        ],
        "err": "PROG: warning: lane 'build' kinds differ (implementer=grok designer=agy tasker=grok). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane build=<kind>[:<model>[:<effort>]]'.\nPROG: warning: lane 'review' kinds differ (reviewer=codex security-reviewer=claude ui-reviewer=agy inspector=agy). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane review=<kind>[:<model>[:<effort>]]'.\n",
        "out": "plan (nothing is written):\n\n<ROOT>/repo/.agents/herdr-agents.conf\n  lane.explore.kind    (unset) → grok\n  panes                (unset) → 4\n  lane.build.roles     (unset) → implementer,designer,tasker\n  lane.explore.roles   (unset) → scouter,researcher\n  lane.review.roles    (unset) → reviewer,security-reviewer,ui-reviewer,inspector\n  max_workers          (unset) → 5\n  split_max_panes      (unset) → 4\n  reuse_workers        (unset) → on\n  lane.build.kind      (unset) → grok\n  lane.build.model     (unset) → grok-4.7\n  lane.build.effort    (unset) → high\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n<ROOT>/repo/.claude/settings.json\n--- a/<ROOT>/repo/.claude/settings.json\n+++ b/<ROOT>/repo/.claude/settings.json\n@@ -1 +1,34 @@\n-{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n+{\n+  \"hooks\": {\n+    \"PreToolUse\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"echo keep-me\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"UserPromptSubmit\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"SessionStart\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ]\n+  }\n+}\n\n",
        "rc": 0
      }
    ]
  },
  "parity: plan dies 2 on an invalid key or value": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--set",
          "nope",
          "1"
        ],
        "err": "PROG: setup --plan: unknown key 'nope'\n",
        "out": "",
        "rc": 2
      },
      {
        "args": [
          "setup",
          "--plan",
          "--set",
          "max_workers",
          "-1"
        ],
        "err": "PROG: setup --plan: invalid value '-1' for max_workers\n",
        "out": "",
        "rc": 2
      },
      {
        "args": [
          "setup",
          "--plan",
          "--panes",
          "5"
        ],
        "err": "PROG: setup --plan: --panes must be 3 or 4\n",
        "out": "",
        "rc": 2
      }
    ]
  },
  "parity: plan dies 2 when a flag is missing its value": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--set",
          "max_workers"
        ],
        "err": "PROG: setup --plan: --set expects a value\n",
        "out": "",
        "rc": 2
      },
      {
        "args": [
          "setup",
          "--plan",
          "--lane"
        ],
        "err": "PROG: setup --plan: --lane expects a value\n",
        "out": "",
        "rc": 2
      },
      {
        "args": [
          "setup",
          "--plan",
          "--lane",
          "--panes",
          "4"
        ],
        "err": "PROG: setup --plan: --lane expects a value\n",
        "out": "",
        "rc": 2
      }
    ]
  },
  "parity: plan dies 4 when the hooks merge would fail": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "not-json\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "not-json\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--panes",
          "4"
        ],
        "err": "PROG: warning: lane 'build' kinds differ (implementer=grok designer=agy tasker=grok). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane build=<kind>[:<model>[:<effort>]]'.\nPROG: warning: lane 'review' kinds differ (reviewer=codex security-reviewer=claude ui-reviewer=agy inspector=agy). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane review=<kind>[:<model>[:<effort>]]'.\nPROG: setup --plan: could not merge hooks into <ROOT>/repo/.claude/settings.json (setup would refuse it and leave the file untouched)\n",
        "out": "plan (nothing is written):\n\n<ROOT>/repo/.agents/herdr-agents.conf\n  lane.explore.kind    (unset) → grok\n  panes                (unset) → 4\n  lane.build.roles     (unset) → implementer,designer,tasker\n  lane.explore.roles   (unset) → scouter,researcher\n  lane.review.roles    (unset) → reviewer,security-reviewer,ui-reviewer,inspector\n  max_workers          (unset) → 3\n  split_max_panes      (unset) → 4\n  reuse_workers        (unset) → on\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n",
        "rc": 4
      }
    ]
  },
  "parity: plan shows the before value of a key being set": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".agents",
          "<dir>"
        ],
        [
          ".agents/herdr-agents.conf",
          "panes=3\n"
        ],
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".agents",
          "<dir>"
        ],
        [
          ".agents/herdr-agents.conf",
          "panes=3\n"
        ],
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--panes",
          "4"
        ],
        "err": "PROG: warning: lane 'build' kinds differ (implementer=grok designer=agy tasker=grok). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane build=<kind>[:<model>[:<effort>]]'.\nPROG: warning: lane 'review' kinds differ (reviewer=codex security-reviewer=claude ui-reviewer=agy inspector=agy). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane review=<kind>[:<model>[:<effort>]]'.\n",
        "out": "plan (nothing is written):\n\n<ROOT>/repo/.agents/herdr-agents.conf\n  panes                3 → 4\n  lane.explore.kind    (unset) → grok\n  lane.build.roles     (unset) → implementer,designer,tasker\n  lane.explore.roles   (unset) → scouter,researcher\n  lane.review.roles    (unset) → reviewer,security-reviewer,ui-reviewer,inspector\n  max_workers          (unset) → 3\n  split_max_panes      (unset) → 4\n  reuse_workers        (unset) → on\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n<ROOT>/repo/.claude/settings.json\n--- a/<ROOT>/repo/.claude/settings.json\n+++ b/<ROOT>/repo/.claude/settings.json\n@@ -1 +1,34 @@\n-{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n+{\n+  \"hooks\": {\n+    \"PreToolUse\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"echo keep-me\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"UserPromptSubmit\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"SessionStart\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ]\n+  }\n+}\n\n",
        "rc": 0
      }
    ]
  },
  "parity: plan the .gitignore entry without writing it": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          ".gitignore",
          "*.log\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          ".gitignore",
          "*.log\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--session-set",
          "lane.build.kind",
          "pi"
        ],
        "err": "",
        "out": "plan (nothing is written):\n\n<ROOT>/repo/.herdr-agents/ws/session.conf\n  lane.build.kind      (unset) → pi\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n<ROOT>/repo/.claude/settings.json\n--- a/<ROOT>/repo/.claude/settings.json\n+++ b/<ROOT>/repo/.claude/settings.json\n@@ -1 +1,34 @@\n-{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n+{\n+  \"hooks\": {\n+    \"PreToolUse\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"echo keep-me\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"UserPromptSubmit\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"SessionStart\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ]\n+  }\n+}\n\n<ROOT>/repo/.gitignore\n--- a/<ROOT>/repo/.gitignore\n+++ b/<ROOT>/repo/.gitignore\n@@ -1 +1,2 @@\n *.log\n+.herdr-agents/\n\n",
        "rc": 0
      }
    ]
  },
  "parity: plan with --set, --user-set and --session-set together": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".agents",
          "<dir>"
        ],
        [
          ".agents/herdr-agents.conf",
          "max_workers=3\n"
        ],
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".agents",
          "<dir>"
        ],
        [
          ".agents/herdr-agents.conf",
          "max_workers=3\n"
        ],
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--set",
          "max_workers",
          "5",
          "--user-set",
          "model.pi.worker",
          "p/m",
          "--session-set",
          "lane.build.kind",
          "pi"
        ],
        "err": "",
        "out": "plan (nothing is written):\n\n<ROOT>/repo/.agents/herdr-agents.conf\n  max_workers          3 → 5\n\n<ROOT>/conf/herdr-agents/config\n  model.pi.worker      (unset) → p/m\n\n<ROOT>/state/ws/session.conf\n  lane.build.kind      (unset) → pi\n\n<ROOT>/repo/AGENTS.md\n--- a/<ROOT>/repo/AGENTS.md\n+++ b/<ROOT>/repo/AGENTS.md\n@@ -1 +1,42 @@\n # Agent instructions\n+\n+<!-- herdr-agents:start -->\n+## Multi-agent workflow (herdr-agents)\n+\n+Inside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\n+the `herdr-agents` skill. The calling agent is the **orchestrator**: it\n+decomposes the objective, writes one brief per slice, spawns role workers in\n+sibling panes, waits on their report files, integrates, runs the gates and\n+owns git. Load the skill (`/herdr-agents`) before planning such work.\n+\n+- **Delegate**: multi-file slices, UI under the design contract, anything\n+  touching auth, secrets or input handling, work that parallelizes, any change\n+  that needs a reviewer, and **research**: reading more than a handful of\n+  files, another repository or several tools' conventions goes to a worker.\n+  The orchestrator briefs it, reads the report and decides.\n+- **Keep**: a one-or-two-file change with no product decision, docs, config,\n+  a question, a quick verification. If writing the brief takes longer than the\n+  change, make the change.\n+- **Briefs are contracts**: goal, expected result, acceptance criteria with\n+  the command that proves each one, decisions already made, owned and\n+  forbidden files, report format. Workers never invent names, flags,\n+  endpoints, credentials or requirements; what the brief leaves open comes\n+  back as an open question and is answered in the next brief.\n+- Workers never commit, push or open PRs; the orchestrator owns git.\n+- The orchestrator is the planner. `spawn planner` opens no pane.\n+- Every code slice gets a reviewer from another model family before push,\n+  including code the orchestrator wrote itself.\n+- The only completion signal is the worker's report file (`dispatch`,\n+  `wait`, `status`); never poll agent state by hand. A busy worker is not a\n+  reason for another pane: `wait`, then dispatch.\n+- Quota (exit 11) stops that worker. Ask the user before switching the\n+  assistant, waiting, taking the slice, or pausing.\n+- How many panes, which assistant and model run each role, and their effort\n+  come from the configuration (`.agents/herdr-agents.conf`, the user file,\n+  the session layer), not from this block: the skill's `explain` and\n+  `config` commands show what is in effect. Project roles override the\n+  skill's in `.agents/herdr-roles/<role>.md`; scratch state lives in\n+  `.herdr-agents/` (git-ignored).\n+- Refresh this block and the hooks by loading `/herdr-agents` and running its\n+  `setup` command from the project root.\n+<!-- herdr-agents:end -->\n\n<ROOT>/repo/.claude/settings.json\n--- a/<ROOT>/repo/.claude/settings.json\n+++ b/<ROOT>/repo/.claude/settings.json\n@@ -1 +1,34 @@\n-{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n+{\n+  \"hooks\": {\n+    \"PreToolUse\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"echo keep-me\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"UserPromptSubmit\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ],\n+    \"SessionStart\": [\n+      {\n+        \"hooks\": [\n+          {\n+            \"type\": \"command\",\n+            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n+          }\n+        ]\n+      }\n+    ]\n+  }\n+}\n\n",
        "rc": 0
      }
    ]
  },
  "parity: plan with an invalid --lane dies 2": {
    "after": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "before": {
      "conf": [],
      "repo": [
        [
          ".claude",
          "<dir>"
        ],
        [
          ".claude/settings.json",
          "{\"hooks\":{\"PreToolUse\":[{\"hooks\":[{\"type\":\"command\",\"command\":\"echo keep-me\"}]}]}}\n"
        ],
        [
          "AGENTS.md",
          "# Agent instructions\n"
        ]
      ],
      "state": []
    },
    "leftovers": [],
    "steps": [
      {
        "args": [
          "setup",
          "--plan",
          "--panes",
          "4",
          "--lane",
          "build=boguskind"
        ],
        "err": "PROG: setup: unknown kind 'boguskind'\n",
        "out": "",
        "rc": 2
      }
    ]
  }
}
