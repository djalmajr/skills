{
  "setup-agents-existing": {
    "files": [
      {
        "content": "# Agent instructions\n\n<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      },
      {
        "content": "{\n  \"hooks\": {\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-claude-separate": {
    "files": [
      {
        "content": "# Agent instructions\n\n<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      },
      {
        "content": "claude-only content\n",
        "rel": "repo/CLAUDE.md"
      },
      {
        "content": "{\n  \"hooks\": {\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: CLAUDE.md exists separately and has no block: run 'setup --target CLAUDE.md' too, or make CLAUDE.md a symlink to AGENTS.md\nPROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-claude-symlink": {
    "files": [
      {
        "content": "# Agent instructions\n\n<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      },
      {
        "content": "{\n  \"hooks\": {\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      },
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block updated: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-dry-run": {
    "files": [
      {
        "content": "# Agent instructions\n",
        "rel": "repo/AGENTS.md"
      },
      {
        "content": null,
        "rel": "repo/.claude/settings.json"
      },
      {
        "content": null,
        "rel": "repo/.gitignore"
      },
      {
        "content": null,
        "rel": "repo/.agents/herdr-agents.conf"
      }
    ],
    "steps": [
      {
        "args": [
          "setup",
          "--dry-run"
        ],
        "err": "",
        "out": "# would write to <ROOT>/repo/AGENTS.md\n<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n\n# would merge into <ROOT>/repo/.claude/settings.json: UserPromptSubmit + SessionStart hooks\n",
        "rc": 0
      },
      {
        "args": [
          "setup",
          "--dry-run",
          "--panes",
          "3",
          "--lane",
          "review=claude:opus:high"
        ],
        "err": "",
        "out": "# would set panes=3 and the preset lanes in <ROOT>/repo/.agents/herdr-agents.conf\n# would apply --lane review=claude:opus:high\n# would write to <ROOT>/repo/AGENTS.md\n<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n\n# would merge into <ROOT>/repo/.claude/settings.json: UserPromptSubmit + SessionStart hooks\n",
        "rc": 0
      }
    ]
  },
  "setup-fresh": {
    "files": [
      {
        "content": "<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      },
      {
        "content": "{\n  \"hooks\": {\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      },
      {
        "content": null,
        "rel": "repo/.gitignore"
      },
      {
        "content": null,
        "rel": "repo/.agents/herdr-agents.conf"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-gitignore": {
    "files": [
      {
        "content": ".herdr-agents/\n",
        "rel": "repo/.gitignore"
      },
      {
        "content": "<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-idempotent": {
    "files": [
      {
        "content": "# Agent instructions\n\n<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      },
      {
        "content": "{\n  \"hooks\": {\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block updated: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      },
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block updated: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-no-abs-path": {
    "files": [
      {
        "content": "# Agent instructions\n\n<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      },
      {
        "content": "{\n  \"hooks\": {\n    \"PreToolUse\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"echo keep-me\"\n          }\n        ]\n      }\n    ],\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      },
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block updated: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-no-hooks": {
    "files": [
      {
        "content": "# Agent instructions\n\n<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      },
      {
        "content": null,
        "rel": "repo/.claude/settings.json"
      }
    ],
    "steps": [
      {
        "args": [
          "setup",
          "--no-hooks"
        ],
        "err": "PROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-panes-3": {
    "files": [
      {
        "content": "panes=3\nlane.build.roles=implementer,designer,tasker\nlane.read.roles=scouter,researcher,reviewer,security-reviewer,ui-reviewer,inspector\nmax_workers=2\nsplit_max_panes=3\nreuse_workers=on\n",
        "rel": "repo/.agents/herdr-agents.conf"
      },
      {
        "content": "<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      },
      {
        "content": "{\n  \"hooks\": {\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      }
    ],
    "steps": [
      {
        "args": [
          "setup",
          "--panes",
          "3"
        ],
        "err": "PROG: warning: lane 'build' kinds differ (implementer=grok designer=agy tasker=grok). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane build=<kind>[:<model>[:<effort>]]'.\nPROG: warning: lane 'read' kinds differ (scouter=grok researcher=grok reviewer=codex security-reviewer=claude ui-reviewer=agy inspector=agy). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane read=<kind>[:<model>[:<effort>]]'.\nPROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "set panes=3\nset lane.build.roles=implementer,designer,tasker\nset lane.read.roles=scouter,researcher,reviewer,security-reviewer,ui-reviewer,inspector\nset max_workers=2\nset split_max_panes=3\nset reuse_workers=on\nremoved role.planner.* and the role kind/model keys of lanes that agreed; divergent lanes kept theirs\nblock written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-panes-4-lane": {
    "files": [
      {
        "content": "lane.explore.kind=grok\npanes=4\nlane.build.roles=implementer,designer,tasker\nlane.explore.roles=scouter,researcher\nlane.review.roles=reviewer,security-reviewer,ui-reviewer,inspector\nmax_workers=3\nsplit_max_panes=4\nreuse_workers=on\nlane.review.kind=claude\nlane.review.model=opus\nlane.review.effort=high\n",
        "rel": "repo/.agents/herdr-agents.conf"
      },
      {
        "content": "<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      },
      {
        "content": "{\n  \"hooks\": {\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      }
    ],
    "steps": [
      {
        "args": [
          "setup",
          "--panes",
          "4",
          "--lane",
          "review=claude:opus:high"
        ],
        "err": "PROG: warning: lane 'build' kinds differ (implementer=grok designer=agy tasker=grok). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane build=<kind>[:<model>[:<effort>]]'.\nPROG: warning: lane 'review' kinds differ (reviewer=codex security-reviewer=claude ui-reviewer=agy inspector=agy). Left the role.*.kind keys in place. Orchestrator: ask the user which kind this lane should use, then run 'setup --lane review=<kind>[:<model>[:<effort>]]'.\nPROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\n",
        "out": "set lane.explore.kind=grok\nset panes=4\nset lane.build.roles=implementer,designer,tasker\nset lane.explore.roles=scouter,researcher\nset lane.review.roles=reviewer,security-reviewer,ui-reviewer,inspector\nset max_workers=3\nset split_max_panes=4\nset reuse_workers=on\nremoved role.planner.* and the role kind/model keys of lanes that agreed; divergent lanes kept theirs\nset lane.review.kind=claude\nset lane.review.model=opus\nset lane.review.effort=high\nblock written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-settings-blank": {
    "files": [
      {
        "content": "{\n  \"hooks\": {\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      },
      {
        "content": "<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-settings-empty": {
    "files": [
      {
        "content": "{\n  \"hooks\": {\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      },
      {
        "content": "<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-settings-entry-hooks-false": {
    "files": [
      {
        "content": "{\n  \"hooks\": {\n    \"SessionStart\": [\n      {\n        \"hooks\": false\n      },\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"echo keep\"\n          }\n        ]\n      },\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-settings-event-false": {
    "files": [
      {
        "content": "{\n  \"hooks\": {\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-settings-invalid": {
    "files": [
      {
        "content": "{invalid\n",
        "rel": "repo/.claude/settings.json"
      },
      {
        "content": "<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: could not merge hooks into <ROOT>/repo/.claude/settings.json\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\n",
        "rc": 4
      }
    ]
  },
  "setup-settings-other": {
    "files": [
      {
        "content": "{\n  \"other\": {\n    \"x\": 1\n  },\n  \"hooks\": {\n    \"PreToolUse\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"echo keep-me\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"bash something-else.sh\"\n          }\n        ]\n      },\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      },
      {
        "content": "<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/AGENTS.md"
      }
    ],
    "steps": [
      {
        "args": [
          "setup"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/AGENTS.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-target-relative": {
    "files": [
      {
        "content": "<!-- herdr-agents:start -->\n## Multi-agent workflow (herdr-agents)\n\nInside Herdr (`HERDR_ENV=1`) non-trivial work in this project runs through\nthe `herdr-agents` skill. The calling agent is the **orchestrator**: it\ndecomposes the objective, writes one brief per slice, spawns role workers in\nsibling panes, waits on their report files, integrates, runs the gates and\nowns git. Load the skill (`/herdr-agents`) before planning such work.\n\n- **Delegate**: multi-file slices, UI under the design contract, anything\n  touching auth, secrets or input handling, work that parallelizes, any change\n  that needs a reviewer, and **research**: reading more than a handful of\n  files, another repository or several tools' conventions is `scouter` work.\n  The orchestrator briefs the scouter, reads the report and decides.\n- **Keep**: a one-or-two-file change with no product decision, docs, config,\n  a question, a quick verification. If writing the brief takes longer than the\n  change, make the change.\n- Workers never commit, push or open PRs; the orchestrator owns git.\n- The orchestrator is the planner. `spawn planner` opens no pane.\n- Roles share a pane by lane (`panes=4`: build, explore, review; `panes=3`:\n  build and read). A busy lane is not a new pane: `wait <lane>`, then dispatch.\n- Every code slice gets a `reviewer` from another model family before push,\n  including code the orchestrator wrote itself (pick that kind by hand).\n- The only completion signal is the worker's report file (`dispatch`,\n  `wait`, `status`); never poll agent state by hand.\n- Quota (exit 11: usage limit, 429, resource exhausted) stops the lane. Ask\n  the user before switching kind, waiting, taking the slice, or pausing.\n- Heavy work (implementation, mechanical edits, research) goes to `grok`\n  first, then `cursor` (grok models), then `codex`, then `claude`; review,\n  security, planning and orchestration stay on `codex`/`claude` (a reviewer\n  is always another model family than the implementer); visual work\n  (`designer`, `inspector`) goes to `agy`.\n- Project roles override the skill's in `.agents/herdr-roles/<role>.md`;\n  project config in `.agents/herdr-agents.conf`; scratch state in\n  `.herdr-agents/` (git-ignored).\n- Refresh this block and the hooks by loading `/herdr-agents` and running its\n  `setup` command from the project root.\n<!-- herdr-agents:end -->\n",
        "rel": "repo/CLAUDE.md"
      },
      {
        "content": null,
        "rel": "repo/AGENTS.md"
      },
      {
        "content": "{\n  \"hooks\": {\n    \"UserPromptSubmit\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] && echo \\\"herdr-agents: this project routes non-trivial work through /herdr-agents — surveys go to a scouter, slices to workers; the orchestrator keeps only one-or-two-file changes.\\\"; true'\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"sh -c '[ \\\"${HERDR_ENV:-}\\\" = 1 ] || exit 0; for script in \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"${CLAUDE_PROJECT_DIR:-$PWD}/.claude/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.agents/skills/herdr-agents/scripts/herdr-agents\\\" \\\"$HOME/.claude/skills/herdr-agents/scripts/herdr-agents\\\"; do [ -f \\\"$script\\\" ] || continue; sh \\\"$script\\\" doctor 2>/dev/null | grep -E \\\"^warn\\\" | sed \\\"s/^warn */herdr-agents doctor: /\\\"; exit 0; done; echo \\\"herdr-agents doctor: skill script not found\\\"; true'\"\n          }\n        ]\n      }\n    ]\n  }\n}\n",
        "rel": "repo/.claude/settings.json"
      }
    ],
    "steps": [
      {
        "args": [
          "setup",
          "--target",
          "CLAUDE.md"
        ],
        "err": "PROG: warning: SessionStart hook cannot resolve herdr-agents; install the skill under the project's or user's .agents/skills or .claude/skills directory\nPROG: warning: project config <ROOT>/repo/.agents/herdr-agents.conf sets neither multi_role, any lane.<name>.kind, nor any role.<role>.kind. max_workers alone is not that choice. Orchestrator: run 'setup --detect', ask the user in their language how many agents at once (4 recommended, or 3) and which detected assistant should implement, review, and research — do not say lane, kind, or panes to them — then run 'setup --panes 3|4 [--lane name=kind:model:effort]'. If doctor reports a missing or legacy config, finish with 'doctor --fix --panes 3|4'.\n",
        "out": "block written: <ROOT>/repo/CLAUDE.md\nhooks written: <ROOT>/repo/.claude/settings.json (UserPromptSubmit reminder, SessionStart doctor)\nstate dir ignored: .herdr-agents/\nnote: Codex, Grok, Cursor and agy read the instruction file; only Claude Code runs the hooks.\n",
        "rc": 0
      }
    ]
  },
  "setup-usage-errors": {
    "files": [
      {
        "content": null,
        "rel": "repo/AGENTS.md"
      },
      {
        "content": null,
        "rel": "repo/.claude/settings.json"
      },
      {
        "content": null,
        "rel": "repo/.agents/herdr-agents.conf"
      }
    ],
    "steps": [
      {
        "args": [
          "setup",
          "--panes",
          "5"
        ],
        "err": "PROG: setup: --panes must be 3 or 4\n",
        "out": "",
        "rc": 2
      },
      {
        "args": [
          "setup",
          "--bogus"
        ],
        "err": "PROG: setup: unknown option '--bogus'\n",
        "out": "",
        "rc": 2
      },
      {
        "args": [
          "setup",
          "--target"
        ],
        "err": "PROG: setup: --target expects a value\n",
        "out": "",
        "rc": 2
      },
      {
        "args": [
          "setup",
          "--panes",
          "--no-hooks"
        ],
        "err": "PROG: setup: --panes expects a value\n",
        "out": "",
        "rc": 2
      }
    ]
  }
}
