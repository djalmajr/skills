# Routes this repo's sessions to ai-memory (auto-capture + recall).
# Operator-local — keep OUT of git (global gitignore should cover .ai-memory.toml).
workspace = "{{WORKSPACE}}"
project   = "{{PROJECT}}"
